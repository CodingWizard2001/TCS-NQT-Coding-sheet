// Calculate the area of circle

#include<iostream>
using namespace std;
int main(){
    int radius;
    cout<<"Enter radius : ";
    cin>>radius;
    cout<<"Area will be : "<<(3.14 * radius * radius);
}